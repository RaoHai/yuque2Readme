"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = browser;

function uploadToS3(id, url, file) {
  return new Promise(function (resolve, reject) {
    var xhr = new XMLHttpRequest();

    xhr.onreadystatechange = function () {
      if (xhr.readyState === 4) {
        if (xhr.status === 200) {
          resolve(id);
        } else {
          reject();
        }
      }
    };

    xhr.open('PUT', url);
    xhr.send(file);
  });
}

function browser(client, file) {
  return client.uploadRequest.create({
    filename: file.name
  }).then(function (_ref) {
    var id = _ref.id,
        url = _ref.url;
    return uploadToS3(id, url, file).then(function () {
      return id;
    });
  });
}